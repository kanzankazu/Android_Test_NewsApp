package com.salt.core.compose.ui

import androidx.compose.ui.graphics.Color

val PrimaryDark = Color(0xffd41484)
val Primary = Color(0xFFD34B9A)
val Accent = Color(0xFF6420AA)

val colorStops = listOf(
    0.0f to PrimaryDark,
    0.2f to Primary,
    1f to Accent
)